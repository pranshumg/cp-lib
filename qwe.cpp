#include <bits/stdc++.h>

using namespace std;

#ifdef PRANSHU_DEBUG
#include "debug.h"
#else
#define dbg(...)
#endif

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  cin >> t;
  while (t--) {
    ${0}
  }
  return 0;
}